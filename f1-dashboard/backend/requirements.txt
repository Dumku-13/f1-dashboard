fastapi==0.138.1
uvicorn[standard]
pydantic

# F1 data
fastf1
pandas
numpy

# HTTP clients
httpx
requests

# Live timing: anonymous SignalR bridge to F1's official feed (routers/livetiming.py)
signalrcore

# AI Race Engineer (routers/engineer.py) — both optional at runtime. Set
# GEMINI_API_KEY or ANTHROPIC_API_KEY; with neither, the engineer falls back to
# a rule-based radio answer and the page still works.
google-genai
anthropic

python-dotenv
defusedxml
